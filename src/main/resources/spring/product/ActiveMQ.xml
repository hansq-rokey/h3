<?xml version="1.0" encoding="UTF-8"?>
<!-- 查找最新的schemaLocation 访问 http://www.springframework.org/schema/ -->
<beans xmlns="http://www.springframework.org/schema/beans"
	xmlns:context="http://www.springframework.org/schema/context"
	xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xmlns:amq="http://activemq.apache.org/schema/core"
	xmlns:jms="http://www.springframework.org/schema/jms"
	xsi:schemaLocation="http://www.springframework.org/schema/beans   
        http://www.springframework.org/schema/beans/spring-beans-4.0.xsd   
        http://www.springframework.org/schema/context   
        http://www.springframework.org/schema/context/spring-context-4.0.xsd
        http://www.springframework.org/schema/jms
        http://www.springframework.org/schema/jms/spring-jms-4.0.xsd
        http://activemq.apache.org/schema/core
        http://activemq.apache.org/schema/core/activemq-core-5.8.0.xsd">

	<amq:connectionFactory id="amqConnectionFactory"
		brokerURL="tcp://10.171.198.241:61616" userName="admin" password="activemq123$%^" />

	<bean id="connectionFactory"
		class="org.springframework.jms.connection.CachingConnectionFactory">
		<constructor-arg ref="amqConnectionFactory" />
		<property name="sessionCacheSize" value="100" />
	</bean>
	
	<!-- ====Consumer side start====-->
	
	<!-- 定义Queue监听器 -->
	<jms:listener-container destination-type="queue" container-type="default" connection-factory="connectionFactory" acknowledge="auto">
		<jms:listener destination="queue.order" ref="mallOrderReceiver"/>
		<jms:listener destination="queue.bom" ref="mallOrderBom" />
		<jms:listener destination="queue.message" ref="mallOrderMessage" />
		<jms:listener destination="queue.ready" ref="mallOrderReady" />
		<jms:listener destination="queue.merchant" ref="merchantMessage" />
		<jms:listener destination="queue.sourceOrder" ref="sourceOrderMessage" />
		<jms:listener destination="queue.activityUser" ref="activityUserMessage" />
	</jms:listener-container>
	
	<!-- ====Consumer side end==== -->
</beans>  